using UnityEngine;

public class FixPivot : MonoBehaviour
{
    private bool isSup;
    private bool isDown;

    public bool IsSup { get { return isSup; } set { isSup = value; } }
    public bool IsDown { get { return isDown; } set { isDown = value; } }
}
